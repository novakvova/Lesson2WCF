<?xml version="1.0" encoding="utf-8" ?>

<configuration>
  <system.serviceModel>

    <services>
      <service name ="Server.Window1">

        <host>
          <baseAddresses>
            <add baseAddress="http://localhost:4000/IContract"/>
          </baseAddresses>
        </host>

        <!-- Указание Адреса, Привязки, Контракта -->
        <endpoint address=""
                  binding="basicHttpBinding"
                  contract="Server.IContract" />
      </service>
    </services>

  </system.serviceModel>
</configuration>